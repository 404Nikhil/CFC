#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define pb push_back
int main() {
   int n;
   cin>>n;
   vector<int> a(n);
   vector<int> b(n);
   for(auto& it : a)
   cin>>it;
    for(auto& it: b)
    cin>>it;
    int m;
    cin>>m;
    vector<int> c(n);
   vector<int> d(n);
    for(auto& it : c)
    cin>>it;
    for(auto& it : d)
    cin>>it;
    return 0;
}